import { AppConfig } from '.'

export default {
  port: 3000,
  apiUrl: '',
  app: {
    htmlAttributes: { lang: 'zh-CN' },
    title: 'React Template',
    titleTemplate: 'React Template - %s',
  },
} as AppConfig
